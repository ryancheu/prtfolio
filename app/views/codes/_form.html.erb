<%= form_for(@code) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <div class="field">
    <%= f.label :gist_id %><br>
    <%= f.text_area :gist_id %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
