<%= bootstrap_form_for(@project) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.text_field :title %>
  <%= f.text_area :description %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
