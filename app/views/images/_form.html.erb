<%= form_for(@image) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :content %><br />
  <%= f.file_field :content %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
